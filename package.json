{
  "name": "seti-v3",
  "version": "0.1.0",
  "private": true,
  "description": "Seti — send mobile money from your stablecoin balance",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --cwd apps/api dev & bun run --cwd apps/web dev",
    "build": "bun run --cwd packages/shared build && bun run --cwd apps/api build && bun run --cwd apps/web build",
    "lint": "bun run --cwd apps/api lint && bun run --cwd apps/web lint",
    "test": "bun run --cwd apps/api test && bun run --cwd apps/web test",
    "db:generate": "bun run --cwd apps/api db:generate",
    "db:migrate": "bun run --cwd apps/api db:migrate",
    "db:studio": "bun run --cwd apps/api db:studio"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.5.0"
  }
}